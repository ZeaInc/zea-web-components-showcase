import { html } from 'lit-html';

/**
 * Primary UI component for user interaction
 */
export const PanelProgressBar = ({ }) => {
  return html`<zea-panel-progress-bar>Loading...</zea-panel-progress-bar>`
};
