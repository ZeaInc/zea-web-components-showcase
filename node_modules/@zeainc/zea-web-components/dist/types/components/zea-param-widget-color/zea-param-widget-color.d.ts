export declare class ZeaParamWidgetColor {
  /**
   * Parameter to be edited
   */
  parameter: any;
  /**
   * The application data
   */
  appData: any;
  /**
   * Color to be displayed in the sample box
   */
  sampleColor: string;
  private container;
  private colorPicker;
  private colorPickerContainer;
  private parameterChange;
  private undoing;
  private sampleTextColor;
  private colorPickerHeight;
  private resizeTimeout;
  constructor();
  /**
   * Listen to window resize event
   */
  handleWindowResize(): void;
  /**
   * Resizes color picker
   */
  resizeColorPicker(): void;
  /**
   * Run when component loads
   */
  componentDidLoad(): void;
  /**
   * Called when the parameter value changes externally
   */
  updateComponent(): void;
  /**
   * Set the color of the text in the sample box
   */
  private setSampleTextColor;
  /**
   * Setup the color picker and it's events
   */
  private setUpColorPicker;
  /**
   * Handles the creation of the UX Change object and the UndoRedoManager
   *
   * @param {Parameter} value - Parameter object
   */
  handleUXChange(value: any): void;
  /**
   * Main ender method.
   * @return {JSX} The generated html
   */
  render(): any;
}
